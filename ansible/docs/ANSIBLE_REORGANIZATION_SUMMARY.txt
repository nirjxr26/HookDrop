#!/usr/bin/env bash

cat << 'EOF'

╔════════════════════════════════════════════════════════════════════════════════╗
║                                                                                ║
║        📦 ANSIBLE FILES - REORGANIZATION AVAILABLE                            ║
║                                                                                ║
║            Clean Structure + Short Names + Easy to Remember                   ║
║                                                                                ║
╚════════════════════════════════════════════════════════════════════════════════╝

CURRENT STATE (Flat Files)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

project-root/
├── ansible_playbook_bootstrap.yaml
├── ansible_role_prerequisites_tasks.yaml
├── ansible_role_prerequisites_defaults.yaml
├── ansible_role_kind_cluster_tasks.yaml
├── ansible_role_kind_cluster_defaults.yaml
├── ansible_role_argocd_tasks.yaml
├── ansible_role_argocd_defaults.yaml
├── ansible_role_kyverno_tasks.yaml
├── ansible_role_kyverno_defaults.yaml
├── ansible_role_sealed_secrets_tasks.yaml
├── ansible_role_sealed_secrets_defaults.yaml
├── ansible_inventory_localhost.yaml
└── ansible.cfg

Pros:  ✓ All visible
       ✓ Easy to review

Cons:  ✗ Hard to navigate
       ✗ Long file names
       ✗ Not scalable
       ✗ Non-standard


NEW STATE (Organized)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

project-root/
└── ansible/
    ├── ansible.cfg
    ├── inventory/
    │   └── default.yaml         ← localhost.yaml
    ├── playbooks/
    │   └── cluster-setup.yaml   ← ansible_playbook_bootstrap.yaml
    └── roles/
        ├── prep/                ← prerequisites
        │   ├── tasks/main.yaml
        │   └── defaults/main.yaml
        ├── cluster/             ← kind_cluster
        │   ├── tasks/main.yaml
        │   └── defaults/main.yaml
        ├── argocd/
        │   ├── tasks/main.yaml
        │   └── defaults/main.yaml
        ├── policy/              ← kyverno
        │   ├── tasks/main.yaml
        │   └── defaults/main.yaml
        └── secrets/             ← sealed_secrets
            ├── tasks/main.yaml
            └── defaults/main.yaml

Pros:  ✓ Standard Ansible pattern
       ✓ Short names (prep, cluster, policy, secrets)
       ✓ Clean hierarchy
       ✓ Easy to navigate
       ✓ IDE-friendly
       ✓ Scalable
       ✓ Professional

Cons:  ✗ One more directory level


NAME MAPPING (Easy to Remember)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

OLD NAME              NEW NAME       WHAT IT DOES
─────────────────────────────────────────────────────────────────────────────
prerequisites        → prep          🛠️  Install tools & prerequisites
kind_cluster         → cluster       ☸️  Create local Kubernetes cluster
argocd               → argocd        📦  GitOps deployment controller
kyverno              → policy        🔒  Policy enforcement engine
sealed_secrets       → secrets       🔐  Secret encryption
bootstrap            → cluster-setup 🚀  Main playbook (orchestration)
localhost.yaml       → default.yaml  📋  Inventory (standard name)


HOW TO REORGANIZE (One Command!)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Read the reorganization guide:
   cat ANSIBLE_FILE_NAMES.md

2. Run reorganization:
   make ansible-reorganize

3. Verify it worked:
   make cluster-up-ansible-check

4. Use it:
   make cluster-up-ansible

That's it! ✨


BEFORE REORGANIZATION (Flat)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ansible-playbook ansible/playbooks/cluster-setup.yaml
ansible-playbook ansible/playbooks/cluster-setup.yaml --tags prep


AFTER REORGANIZATION (Clean)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ansible-playbook ansible/playbooks/cluster-setup.yaml
ansible-playbook ansible/playbooks/cluster-setup.yaml --tags prep


TAG QUICK REFERENCE (After Reorganization)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Install only tools
ansible-playbook ansible/playbooks/cluster-setup.yaml --tags prep

# Create cluster only
ansible-playbook ansible/playbooks/cluster-setup.yaml --tags cluster

# Skip Kyverno policies
ansible-playbook ansible/playbooks/cluster-setup.yaml --skip-tags policy

# Verbose debugging
ansible-playbook ansible/playbooks/cluster-setup.yaml -vvv


MAKE COMMANDS (Always Work)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

make ansible-reorganize         Reorganize files
make ansible-setup              Install Ansible
make cluster-up-ansible         Run bootstrap
make cluster-up-ansible-check   Dry-run
make ansible-status             Check status


FILES TO REVIEW
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📄 ANSIBLE_FILE_NAMES.md        ← START HERE (3 min)
   Quick reference for name mapping and usage

📄 ANSIBLE_REORGANIZATION.md    (10 min)
   Complete reorganization guide with manual steps

📄 README_ANSIBLE.md            (5 min)
   Main entry point with updated documentation


COMPARISON MATRIX
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Aspect              Flat (Before)      Organized (After)
─────────────────────────────────────────────────────────────────────────────
File count in root  12+ files          All in ansible/
Navigation          Hard              Easy
IDE support         Poor              Good
Standard pattern    No                Yes
Scalability         Limited           Excellent
Git history         Messy             Clean
Onboarding          Confusing         Clear
Memorable names     Long              Short (prep, cluster, policy, secrets)


BENEFITS OF REORGANIZATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ Cleaner project root (one ansible/ directory vs 12+ files)
✅ Standard Ansible structure (recognized by tools and teams)
✅ Short memorable names (prep, cluster, policy, secrets)
✅ Better IDE support (proper hierarchy)
✅ Easier to extend (add new roles/playbooks naturally)
✅ Better for git (cleaner diffs and history)
✅ Professional appearance (enterprise-ready structure)
✅ Team-friendly (everyone knows the pattern)
✅ No loss of functionality (all features intact)
✅ Easy rollback (Git has your back)


DETAILED STEPS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Check current status:
   make ansible-status

2. Read the guide:
   cat ANSIBLE_FILE_NAMES.md

3. Reorganize:
   make ansible-reorganize

4. Verify structure:
   find ansible -type f | sort

5. Verify it works:
   make cluster-up-ansible-check

6. Commit changes:
   git add -A
   git commit -m "Reorganize Ansible files: flat → organized with short names"

7. Clean up old files (optional):
   rm -f ansible_*.yaml ansible.cfg organize-ansible.sh setup-ansible-dirs.sh

8. Start using it:
   make cluster-up-ansible


AFTER REORGANIZATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Everything still works!

✅ make ansible-setup            ← Works (auto-detects old or new)
✅ make cluster-up-ansible       ← Works (auto-detects old or new)
✅ make cluster-up-ansible-check ← Works (auto-detects old or new)
✅ Old playbooks still work     ← Not recommended, use new ones
✅ Makefile updated             ← Supports both structures

New preferred way:
├─ cd ansible
├─ ansible-playbook playbooks/cluster-setup.yaml
└─ Use --tags prep, cluster, argocd, policy, secrets


═════════════════════════════════════════════════════════════════════════════════

Ready?

1. cat ANSIBLE_FILE_NAMES.md        ← Quick reference
2. make ansible-reorganize          ← One command
3. make cluster-up-ansible          ← Use it
4. 🎉 Done!

═════════════════════════════════════════════════════════════════════════════════

EOF
