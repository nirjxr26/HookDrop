---
# Sealed Secrets role defaults
sealed_secrets_namespace: "kube-system"
sealed_secrets_release_name: "sealed-secrets"
sealed_secrets_helm_chart: "sealed-secrets/sealed-secrets"
sealed_secrets_helm_repo: "https://bitnami-labs.github.io/sealed-secrets"
